import React from 'react'
import ReactDOM from 'react-dom/client'
import SteamSunApp from './SteamSunApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SteamSunApp />
  </React.StrictMode>,
)
